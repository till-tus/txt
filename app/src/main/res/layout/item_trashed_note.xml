<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:minHeight="112dp"
    android:orientation="vertical"
    android:paddingTop="14dp"
    android:paddingBottom="8dp">

    <TextView
        android:id="@+id/trashedNoteText"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ellipsize="end"
        android:includeFontPadding="false"
        android:maxLines="4"
        android:textColor="@color/launcher_text"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/trashedNoteDeletedAt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:includeFontPadding="false"
        android:textColor="@color/launcher_text_secondary"
        android:textSize="13sp" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="52dp"
        android:gravity="center_vertical|end"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/restoreTrashedNoteButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:gravity="center"
            android:paddingHorizontal="6dp"
            android:text="@string/restore_note"
            android:textColor="@color/launcher_text"
            android:textSize="15sp" />

        <TextView
            android:id="@+id/deleteTrashedNoteButton"
            android:layout_width="0dp"
            android:layout_height="48dp"
            android:layout_weight="1"
            android:gravity="center"
            android:paddingHorizontal="6dp"
            android:text="@string/delete_note_permanently"
            android:textColor="@color/launcher_warning"
            android:textSize="15sp" />

    </LinearLayout>

</LinearLayout>
